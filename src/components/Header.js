function Header(props){
    return(
        <div>
            <h1>{props.heading}</h1>
            <h4>Creative engineers and data scientists building a world where you can belong anywhere</h4>
        </div>
    )
}

export default Header;